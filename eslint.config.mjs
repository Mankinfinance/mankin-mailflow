import { defineConfig, globalIgnores } from "eslint/config";
import nextVitals from "eslint-config-next/core-web-vitals";
import nextTs from "eslint-config-next/typescript";

const eslintConfig = defineConfig([
  ...nextVitals,
  ...nextTs,
  {
    // React Compiler readiness rules. The latest eslint-config-next ships
    // these as errors, but this codebase predates them and the flagged
    // patterns (setState-in-effect, purity) are advisory - fixing them is
    // a separate, behaviour-sensitive effort, not lint hygiene. Keep them
    // visible as warnings so CI can gate on genuine errors without
    // blocking on React Compiler adoption.
    rules: {
      "react-hooks/set-state-in-effect": "warn",
      "react-hooks/purity": "warn",
      "react-hooks/immutability": "warn",
      // Honour the underscore convention for deliberately-unused bindings
      // (e.g. positional params a callback signature forces but doesn't use).
      "@typescript-eslint/no-unused-vars": [
        "warn",
        {
          argsIgnorePattern: "^_",
          varsIgnorePattern: "^_",
          caughtErrorsIgnorePattern: "^_",
          destructuredArrayIgnorePattern: "^_",
        },
      ],
    },
  },
  // Override default ignores of eslint-config-next.
  globalIgnores([
    // Default ignores of eslint-config-next:
    ".next/**",
    "out/**",
    "build/**",
    "next-env.d.ts",
  ]),
]);

export default eslintConfig;
